Django>=5.2,<5.3
Pillow>=10.0

# Remote MCP server + OAuth 2.0 authorization server for claude.ai's
# "Add custom connector" flow. django-mcp-server 0.5.7 was built against
# the mcp SDK's v1 API (FastMCP); the mcp package renamed that to
# MCPServer in its v2 release, so mcp must stay pinned below 2 or
# `import mcp_server` fails immediately with an ImportError.
mcp<2
django-mcp-server>=0.5.7
djangorestframework>=3.15
django-oauth-toolkit>=3.4
